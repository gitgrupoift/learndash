@using LearnDash.Dal.Models
@model LearningFlow

    @if (Model.FlowType == FlowType.Static)
    {
        @Html.Partial("_LinearListView", Model)
    }
    else
    {
        @Html.Partial("_DinamicListView", Model)
    }